import CardInfo from './_components/card-info';

const Page = () => {
    return (
        <main className="h-screen flex justify-center items-center">
            <CardInfo />
        </main>
    );
};

export default Page;
